# 本地开发 env 模板。生产部署见仓库根目录 ../.env.production.example
# 校验：npm run check:production-env-example
#
# PostgreSQL 连接串（Prisma 7 通过 adapter-pg 使用）
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/flower_wms?schema=public"

# Auth.js (NextAuth v5) — 后台员工登录
AUTH_SECRET="请更换为足够长的随机字符串"
NEXTAUTH_SECRET="请更换为足够长的随机字符串"
NEXTAUTH_URL="http://localhost:3000"

# 小程序 / 外网访问 Next 静态资源与 API 的基址（真机联调请改为局域网 IP）
NEXT_PUBLIC_ASSET_BASE_URL="http://192.168.31.200:3000"
NEXT_PUBLIC_API_URL="http://192.168.31.200:3000"

# 微信小程序
WECHAT_MINI_APP_ID=""
WECHAT_MINI_APP_SECRET=""
WECHAT_JWT_SECRET="请更换为足够长的随机字符串"

# DeepSeek AI — 物料智库智能补全（拉丁名 / 养护表格）
DEEPSEEK_API_KEY=""
# 可选，默认 deepseek-chat；若 DeepSeek 提供 v4 pro 模型名请在此覆盖
DEEPSEEK_MODEL="deepseek-chat"

# ── Sprint 14：阿里云 OSS 对象存储 ─────────────────────────────────────────
STORAGE_DRIVER=oss
ENABLE_OSS_UPLOAD=true
ENABLE_LEGACY_UPLOADS=false

ALIYUN_OSS_BUCKET=universe42
ALIYUN_OSS_REGION=oss-cn-hangzhou
ALIYUN_OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_INTERNAL_ENDPOINT=https://oss-cn-hangzhou-internal.aliyuncs.com
# 生产（同地域 ECS）：内网 Endpoint；本地开发（非内网）：外网 Endpoint
ALIYUN_OSS_UPLOAD_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
ALIYUN_OSS_PUBLIC_BASE_URL=https://oss.universe42.studio
ALIYUN_OSS_OBJECT_PREFIX=universe42

ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret

UPLOAD_MAX_SIZE_MB=3
# 业务层单张图片上限（MB）。Nginx client_max_body_size 应大于此值（推荐 5m），见 deploy/nginx/README.md
UPLOAD_ALLOWED_MIME_TYPES=image/jpeg,image/png,image/webp
UPLOAD_ALLOWED_EXTENSIONS=jpg,jpeg,png,webp
UPLOAD_ALLOW_SVG=false

IMAGE_STORAGE_MODE=object_key
IMAGE_RETURN_PUBLIC_URL=true
BLOCK_LOCALHOST_IMAGE_URL=true
NORMALIZE_LOCALHOST_UPLOADS=false

# CMS 客户端图片预览（与 ALIYUN_OSS_PUBLIC_BASE_URL 保持一致）
NEXT_PUBLIC_OSS_PUBLIC_BASE_URL=https://oss.universe42.studio
NEXT_PUBLIC_OSS_OBJECT_PREFIX=universe42
